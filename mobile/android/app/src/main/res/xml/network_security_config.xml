<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: backend runs on local HTTP (not HTTPS). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
